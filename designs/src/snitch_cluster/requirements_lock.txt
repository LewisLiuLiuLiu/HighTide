# PyPI closure for the Snitch clustergen wrapper/pkg generator (clustergen ships
# in @snitch_cluster_src; see MODULE.bazel).  Consumed by
# pip.parse(hub_name = "pip_clustergen").  Resolved from clustergen's declared
# deps (json5/jsonref/mako/jsonschema).  peakrdl is deliberately NOT here: the
# snitch_cluster_peripheral_reg[_pkg].sv files ship pre-generated in the upstream
# archive (setup.sh's peakrdl branch is guarded and never runs), so they are
# referenced from @snitch_cluster_src, not regenerated.
attrs==26.1.0
json5==0.15.0
jsonref==1.1.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
Mako==1.3.12
MarkupSafe==3.0.3
referencing==0.37.0
rpds-py==2026.6.3
typing_extensions==4.16.0
